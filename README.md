To start up:
Open one terminal to the ./frontend/ folder and run the command "yarn start"
Open a second to terminal to the ./backend/ folder and run the command "node server.js"

App will be running on localhost:3000 with the backend running on localhost:5000
